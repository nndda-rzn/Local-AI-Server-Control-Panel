{
  "name": "local-ai-server-control-panel",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo runner untuk Local AI Server Control Panel",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n BE,FE -c blue,green \"npm run dev:be\" \"npm run dev:fe\"",
    "dev:be": "npm run dev --prefix backend",
    "dev:fe": "npm run dev --prefix frontend",
    "start:be": "npm run start --prefix backend",
    "build:fe": "npm run build --prefix frontend",
    "preview:fe": "npm run preview --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "docker:ps": "docker compose ps",
    "docker:rebuild": "docker compose build --no-cache && docker compose up -d"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
